let evaluation = (num1, num2)=>{
    return num1 + num2
}
const person ={
    'firstname':'Joanita',
    'lastname':'Nakityo'
    
 }
 function fetchData(){
     return 'sliced bread'
 }
 module.exports ={
     evaluation,
     person,
     fetchData
 }